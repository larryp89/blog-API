{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "backend" }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "backend/api" }]
}
