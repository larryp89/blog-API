{
  "version": 2,
  "builds": [{ "src": "backend/api/index.js", "use": "@vercel/node" }],

  "routes": [{ "src": "/(.*)", "dest": "backend/api/index.js" }],
  "build": { "command": "npm install && npm run migrate && npm run build" }
}
